services:
  proton-portal:
    build: .
    ports:
      - "8765:8000"      # change 8765 to any free port on your TrueNAS host
    environment:
      - STEAM_API_KEY=${STEAM_API_KEY}
    restart: unless-stopped
